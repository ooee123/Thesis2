#include <stdio.h>

int main(int albra) {
   int beta, gamma;
   beta = 5;
   gamma = 6;
   for (int gggg; gggg < 6; gggg++) {
      int temppo;
      temppo = gggg + beta;
      print(temppo);
   }
   return beta + gamma + albra;
}
