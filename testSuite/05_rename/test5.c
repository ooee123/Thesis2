#include <stdio.h>

int funcA(int alpha) {
   int gggeb;
   int geadb;
   gggeb = 6;
   geadb = 11;
   return gggeb + alpha + geadb;
}

int funcB(int beta) {
   int tttt;
   int alpha;
   int gggeb;
   return beta + tttt + alpha + gggeb;
}
